services:
  backend:
    build:
      context: ./backend
    container_name: nexusai-backend
    env_file:
      - ./backend/.env
    ports:
      - "5000:5000"
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
    container_name: nexusai-frontend
    depends_on:
      - backend
    ports:
      - "80:80"
    restart: unless-stopped
