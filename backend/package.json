{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check routes/auth.js && node --check routes/chat.js && node --check routes/conversations.js && node --check middleware/authMiddleware.js && node --check scripts/createTestUser.js && node --check services/aiService.js && node --check services/demoAiService.js && node --check services/cloudantService.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@ibm-cloud/cloudant": "^0.12.17",
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2"
  }
}
